#include<stdio.h>

#include<math.h>

double multi (double a, double b){
  return a*b;
}
double div (double a, double b){
  if (b==0) return null;
  return a/b;
}
double add (double a, double b){
  return a+b;
}

int main(){

int a = 10; 
int b = 20 

   

printf("%d", multi(a,b) );
printf("%d", add(a,b) );
printf("%d", div(a,b) );

return 0;}
